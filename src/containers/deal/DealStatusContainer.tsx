import React from "react";
import DealStatus from "components/deal/status";

class DealStatusContainer extends React.Component {
  render() {
    return <DealStatus />;
  }
}

export default DealStatusContainer;
